namespace PetHotel.Operations.Application.Medications.GetDayMedications;

/// <summary>Medicações administradas no dia (todas as estadias do tenant) para o painel.</summary>
public sealed record GetDayMedications(DateOnly Date);
