/**
 * Tipos do contrato OpenAPI do backend.
 *
 * NÃO EDITAR À MÃO. Este arquivo é gerado por `pnpm gen:api` a partir do
 * /openapi/v1.json do backend (docs/08 §Type safety com o backend). Mudança de
 * DTO no backend quebra o build do front — é o "teste de arquitetura" do front.
 *
 * Placeholder até a primeira geração (requer o backend rodando).
 */
export interface paths {}
export interface components {
  schemas: Record<string, unknown>;
}
export type webhooks = Record<string, never>;
export type operations = Record<string, never>;
